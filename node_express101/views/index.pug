include header.pug

if entries.length
  each entry in entries
    .panel.panel-default
      .panel-heading
        .text-muted.pull-right
          =entry.published
        =entry.title
      .panel-body
        =entry.body
else
  a(href="/new-entry") Add one!

include footer.pug
